const tilesController = {
    async setUniqueTile(tileMap) {
        for (let y = 0; y < tileMap.data.nbRows; y++) {
            for (let x = 0; x < tileMap.data.nbColumns; x++) {
                tileMap.data.tile[y][i] = new Point(128,54);

                }
            }
    },

}